# To-Do-List-Manager
A simple To-Do List Manager built using Python and MySQL. The project supports role-based access, where Admin users have full control over tasks and users, while Members have limited access to manage their own tasks. Designed to demonstrate basic CRUD operations, authentication, and database integration.
